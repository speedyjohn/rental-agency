server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/RentalAgency
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false